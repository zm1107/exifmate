# 决策记录：项目铁律 —— 远程仓库提交仅含版本信息

- 日期：2026-09-07
- 触发事件：2026-09-07 官网改版时，将开发过程提交（决策记录、开发明细说明）直接推送到了远程 `main`。

## 铁律

推送远程仓库，只提交版本信息，不能包含任何开发者或开发过程信息。规则全文见仓库根目录 `AGENTS.md`。

## 执行流程

1. 日常开发在本地 `dev` 分支，提交信息不限（可详尽记录过程）。
2. 成果就绪后：`git checkout main` → `git merge --squash dev` → 提交信息**只写版本号** `vX.Y.Z`（版本说明留在本地 `dev` 的提交信息里）。
3. 仅推送 `main`；`dev` 与任何过程分支不推送。
4. 若误推过程信息，重写 `main` 历史后 `git push --force-with-lease` 纠正。

## 本次纠正

- `main` 上 `a3f95e7`、`260622b` 两个开发过程提交已压缩为版本提交 `v1.0.0` 并强制更新远程；原始提交仍保留在本地 `dev` 分支。
