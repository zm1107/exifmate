# ExifMate 官网 · 项目铁律

## Git：远程仓库只保留版本信息

- 推送到远程仓库（origin）的每一个提交，其提交信息只能包含**版本信息**：版本号 + 面向用户的版本内容说明。
  - ✅ 示例：`v1.0.0：官网四大核心卖点新版上线`
  - ❌ 禁止：任何开发者信息、开发过程信息（如 WIP、调试记录、决策记录、dev 分支合并说明、开发明细清单等）。
- 日常开发在本地 `dev` 分支进行，提交信息可以详尽，但 `dev` 仅存本地，**不推送远程**。
- 进入远程 `main` 的唯一方式：将 `dev` 成果以 squash 方式压缩为**一条版本提交**（`git merge --squash dev`），再推送。
- 远程历史一经推送即为对外口径；发现误推开发过程信息时，必须重写 `main` 历史并强制更新远程。
